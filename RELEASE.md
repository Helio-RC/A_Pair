# SeatFlow v1.4.0 发布说明

本次发布完成了分发模式从便携式单文件到标准安装包的迁移，引入了 Velopack 自动更新框架、遥测系统和标准化数据存储。

## 新增
- **键盘快捷键系统** — 6 个全局快捷键（Ctrl+Z 撤销、Ctrl+Y 重做、Ctrl+S 保存、Ctrl+滚轮 缩放、Delete 删除选中、Esc 取消选择/关闭弹窗），与 TextBox 内置快捷键自动避让。在设置页面"键盘快捷键"卡片中可按需独立开关
- **初次引导新增快捷键环节** — 启动引导新增 Settings 阶段（3 步），引导新用户了解快捷键开关功能；左下角步骤点改为按阶段呈现（9 个点代替 ~24 个点）
- **滚轮缩放调整** — 改为 Ctrl+滚轮缩放，普通滚轮恢复 ScrollViewer 正常滚动
- **Velopack 安装与自动更新** — 跨平台安装包（Windows Setup.exe、Linux AppImage，macOS 支持计划中，暂无安装包），增量自动更新，双源架构（API 网关 → GitHub 兜底），已下载更新包启动时自动应用
- **遥测系统** — OpenTelemetry 页面浏览/操作事件/性能指标采集，opt-in 同意弹窗，Gzip 批量上报
- **.seatsets 数据打包格式** — 应用数据打包为单个 JSON 归档（分块 SHA256 校验），支持设置页面、双击文件、首次启动三种导入途径，Windows 文件关联注册
- **关于页面重新设计** — 编辑式极简布局：非对称 Hero 区、卡片化链接按钮（GitHub / 官网 / 文档 / Issues）、等宽字体版本信息徽章、双列依赖项网格、氛围背景光晕；打开链接改用 Avalonia 原生 `TopLevel.Launcher`（替代 `Process.Start`），减少杀软误报
- **主页重新设计** — 顶部锚定布局：用户头像+个性化问候同行，文档快捷链接按钮（快速入门/安装指南/使用文档/常见问题），RELEASE.md 嵌入渲染为发布说明卡片；移除 Logo，纯文字排版
- **"仅更新人员数据集"** — 不重新排座，仅用最新学生数据更新已有快照
- **安装时数据迁移** — 安装程序自动复制同目录下的 .seatsets 到应用目录，首次启动自动导入

## 重大变更
- 取消单文件发布（PublishSingleFile），改为标准 dotnet publish 文件夹输出
- 数据存储从 `{exeDir}/AppData/` 迁移到 OS 标准路径
- 日志系统重构：Serilog 结构化日志替代 Debug.WriteLine，文件滚动存储

## 新增
- **文件拖放导入** — 将文件从 OS 文件管理器拖到应用窗口即可导入，拖入时显示全屏遮罩覆盖层（支持时显示蓝色下载图标 + "释放文件以导入"，不支持时显示红色禁止图标 + "该页面不支持此文件类型"）。路由规则：人员管理页支持 `.csv`/`.xlsx`/`.json`，自由点管理支持 `.csv`/`.json`，首页和设置页支持 `.seatsets`，其他页面弹出提示对话框

## 修复
- 修复单文件发布时数据目录被设定为临时目录导致数据丢失的 BUG
- 修复引导系统死锁、竞态、窗口状态不同步等多项问题
- 修复弹窗内容重叠、导入后未自动刷新、教师视角导出列镜像、导出按钮无反应
- 修复遥测同意窗口与引导的冲突
