# SeatFlow v1.4.1 发布说明

本次发布重点改进了人员管理的数据导入体验——导入不再受限于固定模板格式，系统能智能识别任意布局的表格。

## 新增
- **导入表格智能字段识别** — CSV/XLSX 导入不再要求严格的"第1行列名、第2行注释、第3行起数据"模板格式。系统自动扫描所有单元格，通过命中已知字段名（如"姓名""身高""性别"等）的位置推测数据的列式/行式布局，并按推测位置读取数据。支持以下非标准场景：
  - 字段不在第一行的表格
  - 双列/多列名单（如 `| 姓名 | 性别 | 姓名 | 性别 |`）的自动聚合
  - 合并单元格中的字段名
  - 行式布局（字段在首列，数据向右延伸）
  - 数据中间的空白行间隔（连续 ≥2 个空单元格视为结束）
- **模板快速路径** — 使用标准模板的表格自动跳过模糊扫描，直接走高效解析路径，无性能损失
- **字段映射扩展** — 可识别更多字段名变体（如"学生姓名""名字""身高(cm)""前排需求"等）
- **大范围表格确认** — 导入前检测数据范围，超过 70×70 时弹出确认对话框，可选择完全扫描或限制扫描
- **导入失败帮助按钮** — 导入数据为空时弹窗新增"查看帮助"按钮，直接跳转在线文档排查章节

## 修复
- CSV 导入时配置项默认丢弃空白行，导致标准模板注释行丢失、数据行号偏移——修复后保留空白行确保行号对齐
